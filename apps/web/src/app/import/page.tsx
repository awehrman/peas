import { ReactNode } from "react";
import { ImportPageContent } from "@peas/features";

export const dynamic = "force-dynamic";

export default function ImportPageRoute(): ReactNode {
  return <ImportPageContent />;
}
