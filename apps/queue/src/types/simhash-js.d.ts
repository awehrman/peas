declare module "simhash-js" {
  export class SimHash {
    hash(text: string): number;
  }
  
  export class Comparator {
    // Add comparator methods if needed
  }
  
  export class Jenkins {
    // Add Jenkins hash methods if needed
  }
} 