import { createMarkNoteWorkerCompletedAction } from "../track-completion";

// Re-export the action for backward compatibility
export const MarkNoteWorkerCompletedAction = createMarkNoteWorkerCompletedAction();
